---
phase: 3
plan: "03-02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
  - docs/GETTING-STARTED.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/DEVELOPMENT.md
  - docs/TESTING.md
autonomous: true
requirements: ["DX-01"]
must_haves:
  truths:
    - "README opens with newcomer-first plain-English framing (D-02) and tours usage before any API reference."
    - "README contains a dedicated Sync vs Async section (D-10) whose text is copied verbatim from the canonical source in `docs/GETTING-STARTED.md` lines 97–122."
    - "`README.md` and `docs/GETTING-STARTED.md` state the sync/async caveat in word-for-word identical wording (D-14); the canonical caveat sentence is: `Awaiting a sync Try yields the Try instance, not the result`."
    - "README surfaces non-Error normalization (D-07) with a short example using `Non-Error thrown (<type>)` + `cause`."
    - "`.breadcrumbs()` documentation states that configured breadcrumbs are recorded on every terminal method (`.value()`, `.unwrap()`, `.error()`, `.result()`) per D-08."
    - "Every executable snippet in README AND every `docs/*.md` file is tagged with the 03-01 marker (```ts doctest```) and executes in CI via the 03-01 harness from this wave onward."
    - "No mention of the Next.js double-add internal fix anywhere in user-facing docs (D-09)."
    - "`docs/ARCHITECTURE.md` reflects normalization in `execute()` and the consistent breadcrumb path across terminal methods (D-13)."
    - "`docs/CONFIGURATION.md`, `docs/DEVELOPMENT.md`, `docs/TESTING.md` audited against current tsup/eslint/vitest config and test patterns (D-15); no structural rewrites."
  artifacts:
    - path: "README.md"
      provides: "Newcomer-first tour, sync/async section, normalization note, accurate breadcrumb semantics, tagged doctest snippets."
      contains: "ts doctest"
    - path: "docs/GETTING-STARTED.md"
      provides: "Canonical sync/async wording preserved; executable snippets tagged with the marker."
      contains: "Awaiting a sync Try yields the Try instance, not the result"
    - path: "docs/ARCHITECTURE.md"
      provides: "Description of non-Error normalization at execute() and unified breadcrumb recording."
    - path: "docs/CONFIGURATION.md"
      provides: "Matches tsup.config.ts, eslint.config.mjs, tsconfig.json as they actually exist today."
    - path: "docs/DEVELOPMENT.md"
      provides: "Matches current scripts in package.json; no stale references."
    - path: "docs/TESTING.md"
      provides: "Describes current vitest setup including the new doctest harness entry point."
  key_links:
    - from: "README.md Sync vs Async section"
      to: "docs/GETTING-STARTED.md Sync vs Async section"
      via: "identical prose and snippet content copied from GETTING-STARTED as canonical source (D-10 + D-14)"
      pattern: "Awaiting a sync Try yields the Try instance, not the result"
    - from: "README.md + docs/*.md tagged snippets"
      to: "src/__tests__/docs/doctest.test.ts (from 03-01)"
      via: "```ts doctest``` marker consumed by extractDoctests across README and docs/*.md (harness scans both from Wave 1)"
      pattern: "ts doctest"
    - from: "Normalization note"
      to: "src/core/Try.ts normalizeThrown()"
      via: "example demonstrates wrapped.message === 'Non-Error thrown (string)' and wrapped.cause === original"
      pattern: "Non-Error thrown"
---

<objective>
Rewrite `README.md` and audit every file under `docs/` so user-facing documentation reflects the real behavior of `src/` after Phases 1–2. Surface non-Error normalization (D-07) and breadcrumb-recording consistency (D-08). Add the dedicated Sync vs Async section (D-10) reconciled word-for-word with `docs/GETTING-STARTED.md` (D-14) — using the existing GETTING-STARTED wording as the single canonical source. Tag all executable snippets with the 03-01 marker so CI catches future drift; the 03-01 harness already scans `README.md` AND `docs/*.md`, so every tagged snippet runs in CI from this wave onward.

Purpose: Today's README implies `.breadcrumbs()` only records on `.value()` and omits the normalization contract. Fixing the docs is the DX-01 deliverable; tagging snippets is what keeps them fixed.
Output: Updated README + docs, all tagged snippets green under the doctest harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-documentation-examples/03-CONTEXT.md
@.planning/phases/03-documentation-examples/03-01-SUMMARY.md
@.planning/phases/02-reporting-runtime-entry-points/02-CONTEXT.md

@README.md
@docs/GETTING-STARTED.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/DEVELOPMENT.md
@docs/TESTING.md

@src/core/Try.ts
@src/core/reporter.ts
@package.json
@tsup.config.ts
@eslint.config.mjs

<interfaces>
<!-- Behaviors that MUST be documented accurately -->

Non-Error normalization (Phase 2 D-01/D-02; surface per D-07):
```typescript
// src/core/Try.ts normalizeThrown
if (e instanceof Error) return e;
const wrapped = new Error(`Non-Error thrown (${typeof e})`);
wrapped.cause = e;
return wrapped;
```

Breadcrumb recording consistency (Phase 2 D-06; surface per D-08):
`addBreadcrumbsIfConfigured()` is called inside `value()`, `unwrap()`, `error()`, and `result()` when breadcrumbs are configured — not only `value()`.

Canonical Sync vs Async wording (single source of truth, already in `docs/GETTING-STARTED.md` lines 97–122; adopt verbatim for README per D-10 + D-14):

> The library works transparently with both sync and async functions. The key difference is in how you consume the result.
>
> **Async functions** — `await` the terminal method (or the `Try` instance directly):
>
> ```typescript
> // Async function: await the terminal call
> const result = await new Try(asyncFn, arg).value();
>
> // Or await the Try instance itself (works for async functions only)
> const result = await new Try(asyncFn, arg);
> ```
>
> **Sync functions** — do not `await`; call the terminal method directly:
>
> ```typescript
> // Sync function: call terminal method without await
> const result = new Try(JSON.parse, rawString).value();
>
> // Awaiting a sync Try yields the Try instance, not the result
> // This is intentional — use .value() / .unwrap() / .error() instead
> ```
>
> If you are unsure whether a function is async, using `.value()` without `await` is always safe for sync functions, and using `await .value()` is always safe for async functions.

The caveat sentence `Awaiting a sync Try yields the Try instance, not the result` is the canonical form. Do NOT invent alternative wording (e.g., "returns the Try instance"). Task 1 copies this block byte-for-byte into README; Task 2 leaves GETTING-STARTED's wording untouched.

Tracked doctest surface (from 03-01 harness): `README.md` + every `docs/*.md` + `src/__tests__/docs/__fixtures__/*.md`. Tagging a snippet in any of those files means it runs in CI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite README.md — voice, sync/async (copied from GETTING-STARTED), normalization, breadcrumb semantics, tagged snippets</name>
  <files>README.md</files>
  <behavior>
    - Opening section preserves the existing "Think and write in plain English" framing and expands newcomer-first (D-02).
    - New top-of-API "Sync vs Async" section (D-10) sits before or immediately after the first usage tour. Section body is copied BYTE-FOR-BYTE from `docs/GETTING-STARTED.md` lines 97–122. The canonical caveat sentence inside the sync example is `Awaiting a sync Try yields the Try instance, not the result` — do not rephrase.
    - A "Non-Error throws" subsection (short, newcomer-framed per Specific Ideas in CONTEXT) shows a string-throw example and documents `Non-Error thrown (string)` + `error.cause === 'the string'`.
    - `.breadcrumbs()` API entry explicitly states breadcrumbs are recorded on every terminal method (`.value()`, `.unwrap()`, `.error()`, `.result()`) — remove any wording that implies `.value()`-only recording.
    - No mention of the Next.js double-add internal fix (D-09).
    - Every executable TypeScript snippet uses the ```ts doctest``` marker from 03-01; pure-bash install snippets and deliberately-broken pedagogical snippets stay untagged.
    - Imports in tagged snippets use package paths (`@power-rent/try-catch`, `/node`, `/browser`, `/nextjs`) — never `../src/...`.
    - If README grows past ~500 lines, the implementer may (Claude's Discretion under D-02) split the API reference into `docs/API.md` — otherwise keep single-file.
  </behavior>
  <action>
    Rewrite `README.md` in place. Traceability: reference D-02, D-07, D-08, D-09, D-10 in the commit message body. Run the doctest harness after editing to confirm every tagged snippet compiles under the 03-01 aliases and passes with `NoopReporter` + mocked Sentry.

    Specific content updates required:
    - Intro: keep and expand the plain-English framing (D-02). No API reference until after the tour.
    - New section "Sync vs Async" copied verbatim from `docs/GETTING-STARTED.md` lines 97–122 (D-10, D-14). Keep the existing heading level consistent with README's other h2 sections. Tag the two TypeScript fenced blocks inside the copied section with the ```ts doctest``` marker (async path + sync path). Do NOT rewrite the prose — byte-identical copy only. (Adding the ` doctest` token to the info string of the fenced blocks is still byte-identical with GETTING-STARTED AFTER Task 2 applies the same marker to the same blocks in GETTING-STARTED.)
    - New subsection "When your function throws something that isn't an Error" (D-07). One ```ts doctest``` snippet demonstrating: throw `'boom'`, observe `error.message === 'Non-Error thrown (string)'` and `error.cause === 'boom'`.
    - `.breadcrumbs()` doc block: adjust wording to state recording happens on every terminal method (D-08). One tagged snippet using `.error()` or `.result()` to make the point (not only `.value()`).
    - Sweep existing code blocks: add the marker to everything that should run; leave untagged anything that is bash, deliberately broken, or references external state (e.g., real Sentry init).

    Do NOT introduce any Next.js double-add discussion.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/docs/doctest --reporter=basic</automated>
  </verify>
  <done>
    Doctest suite enumerates and passes every tagged README snippet. README conveys the four behavior changes (D-07, D-08, D-10) in newcomer-first voice. Line count under 500 or, if split, `docs/API.md` exists and is referenced from README. The Sync vs Async section text is byte-identical to GETTING-STARTED's (verified in Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tag docs/GETTING-STARTED.md snippets + update docs/ARCHITECTURE.md</name>
  <files>docs/GETTING-STARTED.md, docs/ARCHITECTURE.md</files>
  <behavior>
    - `docs/GETTING-STARTED.md` Sync vs Async section (currently lines 97–122) remains the canonical source. Its prose is unchanged. The TypeScript fenced blocks inside it receive the ```ts doctest``` marker so they execute via the 03-01 harness. This keeps GETTING-STARTED and README byte-identical within that section (D-14).
    - Other executable TypeScript snippets in `docs/GETTING-STARTED.md` also receive the marker where they import from package paths and demonstrate real behavior under `NoopReporter`. Snippets that reference external state (real Sentry init, placeholder env vars, pseudo-code) stay untagged.
    - Imports in tagged snippets use package paths (`@power-rent/try-catch`, `/node`, `/browser`, `/nextjs`) — never `../src/...`. Any drift is corrected.
    - `docs/ARCHITECTURE.md` accurately describes (D-13):
      - Non-Error normalization happening in `execute()` (both sync catch and async `.catch`) with wrapping `new Error('Non-Error thrown (<type>)')` + `cause` preserved.
      - Unified breadcrumb recording: `addBreadcrumbsIfConfigured()` invoked consistently from all four terminal methods (`value`, `unwrap`, `error`, `result`).
      - Reporter interface (`report`, `addBreadcrumbs`, `createWrappedError`) and adapter pattern.
    - No reference to the Next.js double-add internal fix in either file (D-09).
  </behavior>
  <action>
    For `docs/GETTING-STARTED.md`: keep the Sync vs Async section prose exactly as it stands (it is the canonical source Task 1 copied from). Add the ```ts doctest``` marker to the TypeScript fenced blocks inside Sync vs Async so they execute; because Task 1 adds the same marker to the same blocks in README, the two sections stay byte-identical. Tag other executable TypeScript snippets in the file. Fix any non-package-path imports to use `@power-rent/try-catch[/sub]`. Update any other drifted content (entry-point selection, install instructions) to match current `package.json`.

    For `docs/ARCHITECTURE.md`: add or revise sections covering normalization and unified breadcrumb recording. Keep description at architectural granularity — do not duplicate code from `src/`. Cross-reference `src/core/Try.ts` execute() and the reporter interface. If any TypeScript snippets in ARCHITECTURE.md are executable under the harness constraints, tag them with the marker.

    Reference D-08, D-10, D-13, D-14 in the commit message body.
  </action>
  <verify>
    <automated>bash -c 'R=$(sed -n "/^## Sync vs Async$/,/^## /p" README.md | sed "\$d"); G=$(sed -n "/^## Sync vs Async$/,/^## /p" docs/GETTING-STARTED.md | sed "\$d"); [ -n "$R" ] &amp;&amp; [ -n "$G" ] &amp;&amp; [ "$(printf "%s" "$R" | wc -l)" -gt 3 ] &amp;&amp; [ "$(printf "%s" "$G" | wc -l)" -gt 3 ] || { echo "Empty or trivial section extracted — check heading names"; exit 1; }; diff &lt;(printf "%s" "$R") &lt;(printf "%s" "$G")'</automated>
  </verify>
  <done>
    README and GETTING-STARTED Sync vs Async sections are byte-identical and both non-empty (diff verify guards against vacuous pass). ARCHITECTURE.md reflects normalization and breadcrumb-consistency behavior. No D-09 content anywhere. Tagged GETTING-STARTED snippets execute under the 03-01 harness.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audit docs/CONFIGURATION.md, docs/DEVELOPMENT.md, docs/TESTING.md against current tooling</name>
  <files>docs/CONFIGURATION.md, docs/DEVELOPMENT.md, docs/TESTING.md</files>
  <behavior>
    - `docs/CONFIGURATION.md` describes actual `tsup.config.ts`, `eslint.config.mjs`, `tsconfig.json` / `tsconfig.esm.json` settings without exaggeration or omission (D-15).
    - `docs/DEVELOPMENT.md` script list matches the `scripts` block in `package.json` (test, test:watch, build, typecheck, lint, format). Any deprecated scripts removed.
    - `docs/TESTING.md` documents the current vitest setup, the adapter-test mocking pattern, and adds a short paragraph pointing to the doctest harness introduced in 03-01 (marker convention, how to add snippets, which files are scanned).
    - No structural rewrite — surgical corrections only (D-15).
    - Any executable TypeScript snippets in these three files that demonstrate real behavior receive the ```ts doctest``` marker; snippets that are illustrative-only (config fragments, shell commands, file-tree layouts) stay untagged.
  </behavior>
  <action>
    Open each file side-by-side with its source of truth (the config file or `package.json`) and fix drift. In `docs/TESTING.md`, add a "Doc verification harness" subsection (~10 lines) noting that snippets tagged ```ts doctest``` in README and docs/*.md are auto-executed by vitest with `NoopReporter` and mocked `@sentry/*` — point contributors at `src/__tests__/docs/README.md`.

    Tag any demonstrative TypeScript snippet in these files with the marker if it would pass the harness. Do not invent new snippets just to have something to tag.

    Reference D-15 in the commit message body.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic &amp;&amp; grep -Fq "doctest" docs/TESTING.md</automated>
  </verify>
  <done>
    Three docs accurately reflect the current toolchain. TESTING.md links contributors to the doctest harness. All existing tests still pass, including any newly-tagged snippets in these three files.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/docs/doctest` passes; every tagged snippet in README + docs/*.md + the 03-01 seed fixture executes.
- README's Sync vs Async section and `docs/GETTING-STARTED.md`'s are byte-identical (diff returns empty; guard asserts both extracted ranges are non-empty and > 3 lines).
- Grepping the repo for "double-add", "double add", or any mention of the Next.js breadcrumb-duplication internal fix returns zero user-facing hits outside `.planning/`.
- `docs/ARCHITECTURE.md` mentions both `Non-Error thrown` and unified breadcrumb recording across all four terminal methods.
- Full `npm test` green.
</verification>

<success_criteria>
1. README is newcomer-first, includes the Sync vs Async section copied verbatim from GETTING-STARTED, a normalization note, accurate breadcrumb semantics, and every executable snippet is tagged and passes the doctest harness.
2. Canonical caveat sentence is `Awaiting a sync Try yields the Try instance, not the result` and the README + GETTING-STARTED Sync vs Async sections are byte-identical (D-14).
3. Docs accurately describe Phase 2 behavior changes (D-07, D-08) without leaking the internal Next.js fix (D-09).
4. CONFIGURATION / DEVELOPMENT / TESTING match today's tooling (D-15).
5. The 03-01 harness executes tagged snippets from both README AND docs/*.md — no deferred "future flip".
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-examples/03-02-SUMMARY.md` capturing: README structural changes, tagged snippet counts per file, which docs were corrected, and any deferred items.
</output>
