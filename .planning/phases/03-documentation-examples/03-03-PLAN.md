---
phase: 3
plan: "03-03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - examples/README.md
  - examples/comprehensive-examples.ts
  - examples/custom-reporter.ts
  - examples/tsconfig.json
autonomous: true
requirements: ["DX-01"]
must_haves:
  truths:
    - "Every file in `examples/` imports from real package paths (`@power-rent/try-catch`, `/node`, `/browser`, `/nextjs`) — never `../src/...` (D-12)."
    - "Every pattern shown in `examples/comprehensive-examples.ts` matches the current `src/` API surface (D-11) — no stale method names, no pre-Phase-1 `.default()` semantics, no Reporter-interface mismatches."
    - "`examples/custom-reporter.ts` implements the current three-method `Reporter` interface (`report`, `addBreadcrumbs`, `createWrappedError`) correctly."
    - "`examples/README.md` accurately describes what each example demonstrates and how to run it."
    - "`npx tsc -p examples` succeeds; the examples tsconfig uses `paths` to resolve the package name to local `src/` during type-checking."
    - "The project-level `npx tsc --noEmit` (which only includes `src/**/*` per `tsconfig.json`) continues to pass unchanged."
  artifacts:
    - path: "examples/comprehensive-examples.ts"
      provides: "Curated tour of the current Try API, trimmed and modernized from 977 lines."
      contains: "@power-rent/try-catch"
    - path: "examples/custom-reporter.ts"
      provides: "Correct Reporter implementation demonstrating the interface contract."
      contains: "implements Reporter"
    - path: "examples/README.md"
      provides: "Per-file summary + run instructions + note on NoopReporter baseline."
    - path: "examples/tsconfig.json"
      provides: "Standalone tsconfig for the examples directory with `paths` mapping `@power-rent/try-catch[/sub]` to local `src/`; `noEmit: true`."
      contains: "paths"
  key_links:
    - from: "examples/*.ts"
      to: "src/index.ts, src/{node,browser,nextjs}/index.ts"
      via: "package-path imports (D-12) resolved via `examples/tsconfig.json` `paths` during `tsc -p examples`"
      pattern: "from '@power-rent/try-catch"
    - from: "examples/custom-reporter.ts"
      to: "src/core/reporter.ts Reporter interface"
      via: "`implements Reporter` with all three methods"
      pattern: "implements Reporter"
---

<objective>
Full audit and rewrite of the `examples/` directory (D-11, D-12). Replace stale patterns and relative imports with clean, package-path-imported examples that match the current `src/` behavior. Ensures that users who copy-paste from `examples/` land on code that actually compiles and runs.

Purpose: `examples/comprehensive-examples.ts` is 977 lines, uses `../src/...` relative imports, and mixes pre-Phase-1 and current patterns. It is a landmine for new users.
Output: Trimmed, modernized examples that mirror README's API contract and use real package import paths, plus a dedicated `examples/tsconfig.json` so `tsc -p examples` verifies the examples in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-documentation-examples/03-CONTEXT.md
@.planning/phases/03-documentation-examples/03-01-SUMMARY.md

@examples/README.md
@examples/comprehensive-examples.ts
@examples/custom-reporter.ts

@src/core/Try.ts
@src/core/reporter.ts
@src/index.ts
@src/node/index.ts
@src/browser/index.ts
@src/nextjs/index.ts
@package.json
@tsconfig.json

<interfaces>
<!-- Contracts examples MUST adhere to -->

Reporter interface (from `src/core/reporter.ts`) — `examples/custom-reporter.ts` must implement all three methods:
```typescript
export interface Reporter {
  report(error: Error, config: ErrorReportConfig): void;
  addBreadcrumbs(data: Record<string, unknown>, functionName?: string): void;
  createWrappedError(error: Error, message: string): Error;
}
export interface ErrorReportConfig {
  readonly message?: string;
  readonly tags: Readonly<Record<string, string>>;
  readonly breadcrumbData?: Record<string, unknown>;
  readonly functionName?: string;
}
```

Current Try API terminal methods (from `src/core/Try.ts`): `.value()`, `.unwrap()`, `.error()`, `.result()`, `.default(v)`, `.finally(cb)`, `.debug(enabled?)`, `.report(msg)`, `.breadcrumbs(...)`, `.tag(k, v)`, `.tags(record)`.

Package entry points (from `package.json` exports): `@power-rent/try-catch`, `@power-rent/try-catch/node`, `@power-rent/try-catch/browser`, `@power-rent/try-catch/nextjs`.

Root `tsconfig.json` only includes `src/**/*`, so `tsc --noEmit` at the repo root never checks `examples/`. The examples must therefore carry their own `tsconfig.json` to be verified in CI. Do NOT extend the root tsconfig's `include` — that would pull `examples/*.ts` into the build output and break `tsup`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add examples/tsconfig.json + rewrite examples/custom-reporter.ts against the current Reporter interface</name>
  <files>examples/tsconfig.json, examples/custom-reporter.ts</files>
  <behavior>
    - `examples/tsconfig.json` exists with:
      - `"extends": "../tsconfig.json"` (inherit strictness + target).
      - `"compilerOptions"`: `"noEmit": true`, `"moduleResolution": "bundler"` (or `"node"`, matching what resolves the `paths` below), `"baseUrl": "."`, and a `"paths"` entry mapping `"@power-rent/try-catch"` → `["../src/index.ts"]` and `"@power-rent/try-catch/*"` → `["../src/*/index.ts"]`.
      - `"include": ["./*.ts"]` — just this folder, nothing else.
      - `"exclude": ["../node_modules"]`.
    - Running `npx tsc -p examples` succeeds after Task 2 lands; failing on either example file's contents fails the command.
    - `examples/custom-reporter.ts` imports the `Reporter` type from `@power-rent/try-catch` (not `../src/...`) per D-12.
    - Defines a `ConsoleReporter` class that `implements Reporter` and covers all three methods with sensible console output.
    - Optionally (Claude's Discretion under D-11) includes a trivial `NoopReporter` usage snippet showing the baseline — keep it short.
    - Ends with `Try.setDefaultReporter(new ConsoleReporter())` so readers see the registration pattern.
  </behavior>
  <action>
    Create `examples/tsconfig.json` per the shape above. Confirm `paths` resolves correctly by running `npx tsc -p examples` after the file is created but before Task 2 — expect it to succeed on the current stale `custom-reporter.ts` only if its imports already match the package name; if it fails, that's fine because Task 2 cleans it up. The acceptance bar for this task is that running `npx tsc -p examples` after rewriting `custom-reporter.ts` succeeds.

    Rewrite `examples/custom-reporter.ts` as a focused ~60–90 line module: imports from `@power-rent/try-catch`, `ConsoleReporter` implementation, `Try.setDefaultReporter(...)` registration call, and an inline comment explaining that each adapter in `src/adapters/*` and `src/nextjs/SentryReporter.ts` follows the same contract. Do NOT reintroduce any method that is not on the `Reporter` interface today.

    Reference D-11, D-12 in the module header comment and commit message body.
  </action>
  <verify>
    <automated>npx tsc -p examples</automated>
  </verify>
  <done>
    `examples/tsconfig.json` exists with the `paths` entries, and `custom-reporter.ts` implements the exact current `Reporter` contract, imports from the package name, and typechecks via `tsc -p examples`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite examples/comprehensive-examples.ts as a trimmed, modernized tour</name>
  <files>examples/comprehensive-examples.ts</files>
  <behavior>
    - All imports use `@power-rent/try-catch[/sub]` per D-12 — zero `../src/...` imports.
    - Every pattern demonstrated exists on the current `Try` class (D-11): `.value()`, `.unwrap()`, `.error()`, `.result()`, `.default()`, `.finally()`, `.debug()`, `.report()`, `.breadcrumbs()` variants, `.tag()` / `.tags()`, `Try.setDefaultReporter`, `Try.throwThroughErrorTypes`.
    - No references to removed or deprecated APIs. No pre-Phase-1 `.default()` semantics.
    - File is organized into labeled sections matching the README tour order (sync basics → async basics → fallback with `.default()` → reporting + tags → breadcrumbs variants → non-Error normalization → finally/debug → entry-point selection).
    - Includes a short "Non-Error throws" demo (mirrors README D-07 section): throw a string, observe `error.message === 'Non-Error thrown (string)'` and `error.cause === 'boom'`.
    - Includes a "Breadcrumbs record on every terminal method" demo (D-08) touching `.value()`, `.unwrap()`, `.error()`, and `.result()` with a configured breadcrumbs call.
    - File typechecks under `npx tsc -p examples`. Project-level `npx tsc --noEmit` also still passes (unchanged scope: `src/**/*`).
    - Total length substantially under the current 977 lines — target ~300–450 lines, guided by coverage not line quotas.
    - Does NOT reference or demonstrate the Next.js double-add internal fix (D-09).
  </behavior>
  <action>
    Full rewrite. Start from the README's API tour as a structural skeleton and expand each section with one or two runnable, realistically-named functions. Keep synthetic `async function fetchUser(id: number)` style — no real network calls. Group sections with `// === Section: Name ===` comments.

    Use `NoopReporter` as the default reporter at the top of the file so running it produces no Sentry traffic. The `ConsoleReporter` from Task 1 can be referenced in a comment as the swap-in option.

    Reference D-07, D-08, D-09, D-11, D-12 in the module header comment.
  </action>
  <verify>
    <automated>npx tsc -p examples &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    `comprehensive-examples.ts` demonstrates every current `Try` method, uses only package-path imports, and passes both the examples typecheck and the root project typecheck. No stale patterns remain.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite examples/README.md to match the new examples</name>
  <files>examples/README.md</files>
  <behavior>
    - Per-file summary: one short paragraph each for `comprehensive-examples.ts` and `custom-reporter.ts` describing what they demonstrate.
    - Run instructions: show the exact command (likely `npx tsx examples/comprehensive-examples.ts` or equivalent — if tsx is not a dev dep, prefer `ts-node` or document a one-liner; pick what actually works in this repo).
    - Documents that examples are type-checked via `npx tsc -p examples` (referencing the new `examples/tsconfig.json`).
    - Note that examples import from the published package name and resolve to local source during development because of the examples tsconfig `paths` (D-12).
    - Links to `README.md` for the API tour and to `docs/GETTING-STARTED.md` for entry-point selection.
    - No stale references to deleted patterns from the old comprehensive examples file.
  </behavior>
  <action>
    Rewrite `examples/README.md` top-to-bottom. Before documenting the run command, verify which TS runner actually works in this repo (check `package.json` devDependencies — `tsx` is common; fall back to `node --import tsx` or a minimal `vitest` one-shot if nothing is installed). If no runner exists, document the gap and suggest `npm install -D tsx` as a one-line setup step rather than inventing a command that fails.

    Include a short "Type-checking these examples" subsection that points at `npx tsc -p examples`.

    Reference D-11, D-12 in commit message body.
  </action>
  <verify>
    <automated>grep -Fq "@power-rent/try-catch" examples/README.md &amp;&amp; ! grep -Fq "../src/" examples/README.md &amp;&amp; grep -Fq "tsc -p examples" examples/README.md</automated>
  </verify>
  <done>
    `examples/README.md` accurately describes the two example files, gives a working run command (or a one-line setup note if no runner is pre-installed), points contributors at the dedicated examples typecheck command, and never references the old relative-import layout.
  </done>
</task>

</tasks>

<verification>
- `npx tsc -p examples` succeeds (dedicated examples typecheck, using `paths` to resolve the package name).
- `npx tsc --noEmit` at the repo root still succeeds (unchanged include scope).
- Grep for `'../src/` inside `examples/` returns zero matches (D-12).
- Grep for `implements Reporter` in `examples/custom-reporter.ts` returns exactly one match.
- `examples/comprehensive-examples.ts` references every terminal method (`.value`, `.unwrap`, `.error`, `.result`) and every fluent method (`.report`, `.breadcrumbs`, `.tag`, `.tags`, `.default`, `.finally`, `.debug`).
- No mention of the Next.js double-add fix anywhere in `examples/` (D-09).
- `npm test` still green.
</verification>

<success_criteria>
1. Every example imports from `@power-rent/try-catch[/sub]` only (D-12).
2. Every pattern shown exists on the current `Try` class and `Reporter` interface (D-11).
3. `custom-reporter.ts` is a correct, minimal implementation of today's `Reporter`.
4. `examples/README.md` accurately describes the files and provides a working run instruction plus the `tsc -p examples` typecheck command.
5. `examples/tsconfig.json` with `paths` entries exists and makes `npx tsc -p examples` the canonical verification gate for the examples folder.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-examples/03-03-SUMMARY.md` capturing: final line counts, which old patterns were dropped, run command chosen, examples tsconfig shape, and any follow-ups (e.g., `tsx` dev dep).
</output>
