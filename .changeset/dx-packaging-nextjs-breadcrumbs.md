---
'@power-rent/try-catch': minor
---

Fix Next.js double-breadcrumbs, safe non-Error throw normalization, optional
`@sentry/*` peer dependencies, ESM packaging + `LICENSE`, ~85% smaller tarball,
and a default export on the main entry (`import Try from '@power-rent/try-catch'`).
